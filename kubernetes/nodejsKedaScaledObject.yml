apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: nodeapp-deployment
  namespace: default
spec:
  scaleTargetRef:
    name: nodeapp-deployment
  minReplicaCount: 1
  maxReplicaCount: 10
  triggers:
  - type: metrics-api
    metadata:
      targetValue: 1
      url: "http://ml-api-service/predict"
      valueLocation: 'predicted_replicas'
  